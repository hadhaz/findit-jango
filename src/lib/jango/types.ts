export type Menu = {
  name: string;
  path: string;
};

export type UserMenu = {
  name: string;
  icon: string;
  path: string;
};

export type Feature = {
  name: string;
  description: string;
  img: string;
};

export type Feedback = {
  name: string;
  img: string;
  score: number;
};
